// we can parse a string, converting it to a number using parteInt function

let strNum = "10";
let parsedNum = parseInt(strNum);

console.log(strNum, typeof (strNum));
console.log(parsedNum, typeof (parsedNum));