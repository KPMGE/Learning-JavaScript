// we can replace a certain pattern on a string by using the replace(method)

let name = "Kevin Carvalho de Jesus Santos";
// replacing 'santos' to a void string
let realName = name.replace("Santos", "");

console.log(realName);