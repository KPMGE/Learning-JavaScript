// there is a property on every string, it is 'length'

let name = "Kevin Carvalho de Jesus";
console.log("length of string: ", name.length);

// to access each element from a string, we can use the same notation from arrays

console.log("The first letter from my name is: ", name[0]);
