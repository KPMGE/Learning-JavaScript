// we can convert an array to a string by using the toString method

let days = [21, 12, 31, 5, 6];
console.log(days);

// converting to string
let stringDays = days.toString();

console.log(stringDays);
