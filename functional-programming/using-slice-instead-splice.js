// the splice function is more recommended because it doesn't modify the original array

let array = [1, 4, 45, 72, 35, 61];

// displaying 3 first elements
console.log(array.slice(0, 3));
console.log(array);
